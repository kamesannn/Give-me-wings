# How to play
# 1. Install kivy using terminal
For windows
1. python -m pip install --upgrade pip setuptools virtualenv
2. python -m virtualenv kivy_venv
3. source kivy_venv/Scripts/activate
4. python -m pip install "kivy[full]"
# 2. Run main.py using PyCharm